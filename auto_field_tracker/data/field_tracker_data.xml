<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!--
        Demo tracker configs — automatically track common models.
        These are created with noupdate="1" so they won't override
        admin customisations after the first install.
    -->

    <!-- Example: Track Sale Orders -->
    <!-- Uncomment to activate on install:

    <record id="tracker_sale_order" model="field.tracker.config">
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="active">True</field>
        <field name="log_on_create">True</field>
        <field name="log_on_write">True</field>
        <field name="log_on_unlink">False</field>
        <field name="track_all_fields">False</field>
    </record>

    -->

</odoo>
